{
  "name": "Unity-Generator Devcontainer",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "backend",
  "workspaceFolder": "/workspaces/Unity-Generator",
  "runServices": ["backend", "frontend"],
  "shutdownAction": "stopCompose",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.debugpy",
        "ms-azuretools.vscode-docker",
        "ms-azuretools.vscode-containers",
        "Vue.volar"
      ]
    }
  },
  "postCreateCommand": "apt-get update && apt-get install -y nodejs npm python3 python3-pip && npm install -g @tauri-apps/cli && cd backend && pip install -r requirements.txt"
}
