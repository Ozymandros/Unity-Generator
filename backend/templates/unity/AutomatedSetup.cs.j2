// Auto-generated by Unity Generator - DO NOT EDIT MANUALLY
// This script is injected temporarily and removed after execution.

using UnityEditor;
using UnityEngine;
using System;

namespace AutoGenerated
{
    /// <summary>
    /// Main entry point for Unity batch-mode automation.
    /// Called via -executeMethod AutoGenerated.ProjectInitializer.Setup
    /// </summary>
    public static class ProjectInitializer
    {
        [MenuItem("Tools/AutoGenerated/Run Setup")]
        public static void Setup()
        {
            Debug.Log("[ProjectInitializer] Starting automated setup...");

            try
            {
                {% if install_packages and packages|length > 0 %}
                Debug.Log("[ProjectInitializer] Step: Install UPM packages");
                PackageInstaller.InstallAll();
                {% endif %}

                {% if generate_scene %}
                Debug.Log("[ProjectInitializer] Step: Generate default scene");
                SceneGenerator.CreateDefaultScene("{{ scene_name }}");
                {% endif %}

                {% if setup_urp %}
                Debug.Log("[ProjectInitializer] Step: Configure project settings (URP)");
                ProjectSettingsConfigurator.ConfigureURP();
                {% endif %}

                Debug.Log("[ProjectInitializer] Step: Validate imports");
                ImportValidator.ValidateAll();

                Debug.Log("[ProjectInitializer] Automated setup completed successfully.");
            }
            catch (Exception ex)
            {
                Debug.LogError($"[ProjectInitializer] Setup failed: {ex.Message}\n{ex.StackTrace}");
                EditorApplication.Exit(1);
                return;
            }
        }
    }
}
